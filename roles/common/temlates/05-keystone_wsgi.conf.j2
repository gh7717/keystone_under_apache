# ************************************
# Vhost template in module puppetlabs-apache
# Managed by Puppet
# ************************************

<VirtualHost *:{{port}}>
  ServerName {{fqdn}}

  ## Vhost docroot
  DocumentRoot "/usr/lib/cgi-bin/keystone"

  ## Directories, there should at least be a declaration for /usr/lib/cgi-bin/keystone

  <Directory "/usr/lib/cgi-bin/keystone">
    Options Indexes FollowSymLinks MultiViews
    AllowOverride None
    Require all granted
  </Directory>

  ## Logging
  ErrorLog "/var/log/apache2/keystone_wsgi_{{role}}.log"
  ServerSignature Off
  CustomLog "/var/log/apache2/keystone_wsgi_{{role}}_access.log" "%h %l %u %t \"%r\" %>s %b %D \"%{Referer}i\" \"%{User-Agent}i\""
  WSGIDaemonProcess keystone_{{role}} display-name=keystone-{{role}} group=keystone processes=2 threads=3 user=keystone
  WSGIProcessGroup keystone_{{role}}
  WSGIScriptAlias / "/usr/lib/cgi-bin/keystone/{{role}}"

  ## Custom fragment
LimitRequestFieldSize 81900
</VirtualHost>
